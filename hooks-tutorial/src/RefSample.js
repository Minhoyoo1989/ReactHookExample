// import { useRef } from "react";

// const RefSample = () => {
//   const id = useRef(1);
//   const setId = (n) => {
//     id.current = n;
//   };
//   const printId = () => {
//     console.log(id.current);
//   };

//   return <div>RefSample</div>;
// };

// export default RefSample;

// *이렇게 작성을 할 경우에는 ref값이 바뀌어도 렌더링이 되지 않는다는 점에 주의, 렌더링과 관련되지 않는 값을 관리 할 때 이와 같은 방법으로 사용하면 된다.
// * 안쓰기 때문에 주서처리해놓음
